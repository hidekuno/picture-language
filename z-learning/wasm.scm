;
; this is a sample data from web assembly
;
; hidekuno@gmail.com
;
(begin
  (load-url (string-append "sicp/" "abstract-data.scm"))
  (load-url (string-append "sicp/" "painter.scm"))
  (load-url (string-append "sicp/" "segments-fish.scm"))
  (load-url (string-append "sicp/" "segments.scm"))
  (load-url (string-append "sicp/" "fractal.scm"))
  (load-url (string-append "z-learning/" "image-url.scm")))

